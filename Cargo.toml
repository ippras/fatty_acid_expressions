[package]
edition = "2024"
name = "fatty_acid_expressions"
version = "0.2.0"

[dependencies]
const_format = "0.2.36"

egui_l10n = { git = "https://github.com/ippras/egui_l10n", branch = "0.6", features = [
    "macros",
] }

[features]
default = ["en", "ru"]
en = []
ru = []
